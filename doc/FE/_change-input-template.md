# 백엔드 변경 입력 자료 (인계 분석용 템플릿)

> 사용법: 이 파일을 복사해 변경 건마다 채우세요.
> 채운 뒤 Claude Code(SilverBridgeFe 디렉토리에서 실행)에게 그대로 붙여넣고
> "이 백엔드 변경의 프론트 영향을 분석하고 doc/FE에 인계 문서 만들어줘"라고 하면 됩니다.
> 규칙: 분석+문서 전용 / 프론트 코드 수정 금지 / git 금지 / 산출물은 /home/skarndaudwls/SilverBridgeBe/doc/FE/.
>
> 복사 추천 파일명: change-input-<주제>-YYYY-MM-DD.md  (예: change-input-login-2026-06-01.md)

---

## 0. 기본 정보
- 변경 주제(한 줄): 
- 작성일: YYYY-MM-DD
- 백엔드 배포 예정일: 
- 관련 티켓/PR(있으면): 

## 1. 영향 받는 엔드포인트
> 메서드 + 전체 경로. 여러 개면 모두.

| Method | Endpoint | 비고 |
|--------|----------|------|
| POST   | /api/... |      |
|        |          |      |

## 2. 무엇이 어떻게 바뀌나 (기존 → 변경)
> 아래 중 해당되는 것만 채우면 됩니다.

### 2-A. 요청(Request) 변경
- 추가/삭제/이름변경된 필드: 
- 필수/선택 변경: 
- 형식·검증 규칙 변경: 

### 2-B. 응답(Response) 변경
> 상태코드별로. 메시지가 있으면 정확한 문구 그대로 적어주세요(프론트가 그대로 표시할 수 있음).

| 상태코드 | 기존 | 변경 후 | 응답 메시지(정확히) |
|----------|------|---------|---------------------|
| 200      |      |         |                     |
| 400      |      |         |                     |
| 404      |      |         |                     |
| 409      |      |         |                     |
| 429      |      |         |                     |

- 에러 응답 body 형태: `{ code, message }` / `{ code, message, data }`(CommonResponse) / 기타: 
  > ※ 프론트 errorHandler는 `error.response.data.message`를 1순위로 읽음. 메시지가 다른 필드면 미리 알려주세요.
- 429일 때 대기시간 제공 여부: `Retry-After` 헤더 / body 필드 / 없음

### 2-C. 동작·정책 변경 (코드/메시지 외)
- 인증/권한 변경: 
- 부수효과(쿠키, 토큰, 리다이렉트 등): 

## 3. 변경 배경 (왜 바꾸나)
> 제가 UX 제안·회귀 판단·우선순위를 정할 때 쓰는 근거입니다. 한두 줄이라도 꼭.
- 

## 4. 알고 있는 제약/주의
- 프론트가 반드시 지켜야 할 것: 
- 하위호환 여부(구버전 프론트가 잠시 공존하는지): 
- 그 외: 

## 5. 분석 시 특별히 봐줬으면 하는 점 (선택)
- 

---
### Claude에게 보낼 때 덧붙일 한 줄 (복붙용)
```
위 입력 자료로 PHASE 0(현재 프론트 구조 파악) 요약부터 보여주고, 내가 "시작해" 하면
기존 인계 문서(/home/skarndaudwls/SilverBridgeBe/doc/FE/backend-change-impact-password-reset-2026-05-22.md)와
같은 구조로 /home/skarndaudwls/SilverBridgeBe/doc/FE/에 인계 문서 1개를 만들어줘.
```
